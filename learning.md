## Notes

- Setup of vite via docs tailwind and use of react only with JS
- Component based as it is Function in JS
- SPA - mutli components navbar,sidebar,hero bar
- Single Page Application
- Import adn Export as i created component in the eg-1 with Compo File
- pass data from component from APP level to compo level just pass data using variable
- Named export vs default export
- Props Drilling pass data from one component to another component top to bottom
- Light Dark theme using special Hook called useState special variable stored in it
- Store and update the commponent rerender
- useEffect is called at last first component is rendered
- Component render -> JSX element -> UseEffect
- Component -> Render Fake data Shimmer UI -> API Fetch -> Render real data
- Component Dependency rerender if u have put in dependency
- React Router Dom and Creating a Router
